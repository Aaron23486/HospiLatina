@model IEnumerable<dynamic>

<div class="card mt-4">
    <div class="card-header">
        <h2>Top 2 de Horarios con Más Pacientes Atendidos</h2>
    </div>
    <div class="card-body">
        <table id="topHorariosTable" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Día</th>
                    <th>Hora</th>
                    <th>Cantidad de Pacientes</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Dia.ToShortDateString()</td>
                        <td>@item.Hora</td>
                        <td>@item.CantidadPacientes</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#topHorariosTable').DataTable({
                "order": [[2, "desc"]],
                "paging": true,
                "searching": true,
                "lengthChange": true,
                "pageLength": 2,
                "info": false,
                "autoWidth": false,
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.10.21/i18n/Spanish.json"
                }
            });
        });
    </script>
}
