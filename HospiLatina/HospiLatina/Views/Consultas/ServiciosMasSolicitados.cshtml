@model IEnumerable<dynamic>

<div class="card mt-4">
    <div class="card-header">
        <h2>Servicios Ordenados por los Más Solicitados</h2>
    </div>
    <div class="card-body">
        <table id="serviciosSolicitadosTable" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Servicio</th>
                    <th>Veces Solicitado</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Servicio</td>
                        <td>@item.VecesSolicitado</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#serviciosSolicitadosTable').DataTable({
                "order": [[1, "desc"]],
                "paging": true,
                "searching": true,
                "lengthChange": true,
                "pageLength": 10,
                "info": false,
                "autoWidth": false,
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.10.21/i18n/Spanish.json"
                }
            });
        });
    </script>
}
