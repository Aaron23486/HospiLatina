@model IEnumerable<dynamic>

<div class="card mt-4">
    <div class="card-header">
        <h2>Top 5 de Doctores con Más Pacientes Atendidos</h2>
    </div>
    <div class="card-body">
        <table id="topDoctoresTable" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Doctor</th>
                    <th>Cantidad de Pacientes Atendidos</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Doctor</td>
                        <td>@item.CantidadPacientes</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#topDoctoresTable').DataTable({
                "order": [[1, "desc"]],
                "paging": true,
                "searching": true,
                "lengthChange": true,
                "pageLength": 5,
                "info": false,
                "autoWidth": false,
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.10.21/i18n/Spanish.json"
                }
            });
        });
    </script>
}
